import 'package:flutter/material.dart';

class PaymentGetawayScreen extends StatelessWidget {
  const PaymentGetawayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: FlutterLogo(size: 100),
      ),
    );
  }
}
